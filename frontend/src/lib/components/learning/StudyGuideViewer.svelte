<script lang="ts">import SourceLinks from './SourceLinks.svelte';import type { SourceReference } from '$lib/source-links';type Section={heading:string;body:string;sources:SourceReference[]};let { sections, sourceCommit }: { sections: Section[]; sourceCommit:string }=$props();</script>
<article class="guide" aria-labelledby="guide-heading"><span class="eyebrow">Study guide</span><h3 id="guide-heading">Request Lifecycle Study Guide</h3><nav aria-label="Guide sections">{#each sections as section,index}<a href={`#guide-${index}`}>{section.heading}</a>{/each}</nav>{#each sections as section,index}<section id={`guide-${index}`}><h4>{section.heading}</h4><p>{section.body}</p><SourceLinks sources={section.sources} {sourceCommit} compact /></section>{/each}</article>
<style>.guide{border:1px solid var(--border);border-radius:1rem;background:var(--panel);padding:clamp(1rem,3vw,2rem)}.guide h3{font-size:2rem;margin:.5rem 0 1rem}.guide nav{display:flex;gap:.5rem;flex-wrap:wrap;padding-bottom:1rem;border-bottom:1px solid var(--border)}.guide nav a{border:1px solid var(--border);border-radius:999px;padding:.4rem .7rem;color:var(--secondary);text-decoration:none;font-size:.72rem}.guide section{padding:1rem 0;border-bottom:1px solid var(--border)}.guide h4{font-size:1.15rem;margin:0 0 .5rem;color:var(--accent)}.guide p{color:var(--secondary);line-height:1.7;max-width:80ch}</style>
